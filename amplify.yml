version: 1
frontend:
  phases:
    # my build commands
     preBuild:
      commands: []
     build:
      commands: []
  artifacts:
    # build artifacts
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: .
