version: 1
frontend:
  phases:
    # my build commands
    build:
      commands:
        - npm run build:$API
        - echo "API_PATH=$API" >> backend/.env 
  artifacts:
    # build artifacts
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
