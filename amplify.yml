version: 1
frontend:
  phases:
    # my build commands
     preBuild:
      commands:
        - npm ci
     build:
      commands:
        - npm run build
  artifacts:
    # build artifacts
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: .
